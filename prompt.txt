You are a senior TypeScript architect performing a deep codebase audit.

Goal: Understand EXACTLY how FreeLLMAPI discovers, syncs, stores, categorizes, and routes models.

Do NOT modify code.

Only analyze and explain.

I want a complete architecture report with file references.

Investigate the entire codebase and answer the following:

# SECTION 1 — MODEL DISCOVERY FLOW

Trace the complete lifecycle of a model from provider to dashboard.

Answer:

1. Where are models initially discovered?
2. Which files fetch model catalogs?
3. Which providers support automatic model discovery?
4. Which providers use hardcoded catalogs?
5. Which providers query remote APIs for models?
6. Which functions create/update models in the database?
7. Which database tables store models?
8. How are model updates handled?
9. How does catalog sync work?

Show exact call chain:

Provider API
→ Sync Service
→ Database
→ API
→ Dashboard

with filenames and functions.

---

# SECTION 2 — CHAT MODEL DETECTION

Explain why chat models automatically appear when API keys are added.

Find:

* functions responsible
* files responsible
* database writes
* provider capability checks

Answer:

1. What triggers chat model discovery?
2. Is discovery performed when a key is added?
3. Is discovery performed during catalog sync?
4. Are models fetched from provider APIs or local catalogs?
5. Why do Gemini models automatically appear?

---

# SECTION 3 — MEDIA MODEL DETECTION

Investigate:

* TTS
* Speech-to-Text
* Image Generation

Answer:

1. Are media models automatically discovered?
2. If yes, where?
3. If no, why not?
4. Which files decide whether a model is:

   * chat
   * image
   * audio
   * transcription
5. Which database tables store media models?
6. What code populates media_models?
7. Why would Chat Models appear automatically while TTS/STT models do not?

Show exact code path.

---

# SECTION 4 — GOOGLE PROVIDER ANALYSIS

Inspect the Google provider implementation.

Find:

* model listing endpoints
* Gemini model discovery logic
* capability detection logic

Answer:

1. Does the Google adapter fetch available models?
2. Does it read supportedGenerationMethods?
3. Can TTS models be discovered from Google's API?
4. Can transcription models be discovered?
5. Is there code currently ignoring media-capable models?
6. Could automatic TTS/STT discovery be implemented without a custom catalog?

Provide evidence from code.

---

# SECTION 5 — OFIS MEDIA CATALOG ANALYSIS

Inspect:

server/src/catalog/ofis-media.ts

Answer:

1. How is this catalog injected?
2. Which function runs it?
3. When does it run?
4. Does catalog sync overwrite it?
5. Why does it survive official catalog updates?
6. Could this approach safely support:

   * Audio
   * STT
   * Image
   * Future media models

Explain risks.

---

# SECTION 6 — ROUTING & FALLBACK

Investigate routing.

Answer:

1. How are chat requests routed?
2. How are audio requests routed?
3. How are transcription requests routed?
4. How are image requests routed?
5. Is fallback supported for media models?
6. What conditions trigger fallback?
7. Can multiple TTS models participate in fallback?
8. Can multiple STT models participate in fallback?

Show exact files and functions.

---

# SECTION 7 — DATABASE SCHEMA

List every relevant table:

* models
* media_models
* api_keys
* fallback_config
* requests
* any other relevant tables

For each table explain:

* purpose
* important columns
* which code reads it
* which code writes it

---

# SECTION 8 — DASHBOARD FLOW

Explain how dashboard pages are populated:

* Chat Models
* Audio
* Speech-to-Text
* Image

Answer:

1. Which API endpoint feeds each page?
2. Which DB queries are used?
3. Which filters determine category visibility?
4. Why would a model appear in Chat but not Audio?

---

# SECTION 9 — IMPROVEMENT OPPORTUNITIES

Based on the current architecture:

1. What is the cleanest way to implement automatic TTS detection?
2. What is the cleanest way to implement automatic STT detection?
3. What is the cleanest way to implement automatic image model detection?
4. Should OFIS Media Catalog remain?
5. Should it be merged with official catalog sync?
6. What would you redesign?

---

# OUTPUT FORMAT

Produce:

1. Executive Summary
2. Architecture Diagram (ASCII)
3. Discovery Flow Diagram
4. Media Flow Diagram
5. Database Diagram
6. Provider Capability Matrix
7. Root Cause Analysis:
   "Why Chat Models Auto Detect But TTS/STT Did Not"
8. Recommendations

Cite filenames, functions, classes, and code snippets wherever possible.

Do not skip any file that participates in model discovery, catalog sync, media model handling, routing, or dashboard rendering.
